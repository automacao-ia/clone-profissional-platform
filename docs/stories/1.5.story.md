# Story E1.5 — Schema de Banco — users e subscriptions

**Epic:** E1 — Autenticação e Gestão de Conta
**Status:** InProgress
**Agente:** @data-engineer (Dara)
**Sprint:** 1

---

## Acceptance Criteria

- [ ] Migration `001_profiles.sql` cria tabela `profiles` linkada ao `auth.users`
- [ ] Migration `002_clones_table.sql` cria tabela `clones` (para E1 enforcement de limites)
- [ ] RLS habilitado: usuário só vê/edita seus próprios dados
- [ ] Trigger `handle_new_user()` cria profile automático no registro
- [ ] Indexes em `profiles(id)` e `clones(user_id)`

## Schema

```sql
-- profiles
id uuid (FK auth.users)
name text
avatar_url text
plan text DEFAULT 'free' CHECK (plan IN ('free','pro','agency'))
created_at timestamptz DEFAULT now()

-- clones
id uuid DEFAULT gen_random_uuid()
user_id uuid (FK profiles.id)
name text NOT NULL
status text DEFAULT 'draft'
created_at timestamptz DEFAULT now()
updated_at timestamptz DEFAULT now()
```

## File List

- `supabase/migrations/001_profiles.sql`
- `supabase/migrations/002_clones_table.sql`
